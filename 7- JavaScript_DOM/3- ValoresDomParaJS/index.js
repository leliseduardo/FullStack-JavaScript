// function register(element) {
//   const nome = element.children.usernameName.value; // Pelo atributo name
//   // const nome = element.children.username.value;  // Pelo atributo id
//   const senha = element.children.password.value;
//   const confirmacao = element.children.passwordConfirmation.value;

//   if (senha == confirmacao) alert("Usuário " + nome + " registrado!");
//   else alert("As senhas não conferem!");
// }

function register(element) {
  console.log(element);
}
